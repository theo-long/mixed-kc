[project]
name = "mixed-kc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dd>=0.6.0",
    "ipython>=9.9.0",
    "scipy>=1.16.3",
]

[dependency-groups]
dev = [
    "ruff>=0.14.10",
]

[tool.ruff.lint]
extend-select = ["I"]
